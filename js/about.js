(function () {
  document.addEventListener('DOMContentLoaded', () => {
    const emailEl = null; // about page has no footer; contact.js
    // already wires #contact-seal independently, nothing extra needed here.
  });
})();
